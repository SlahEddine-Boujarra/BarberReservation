{% extends 'base.html.twig' %}

{% block title %}Barber{% endblock %}

{% block body %}
    <h1>Barber</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ barber.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ barber.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ barber.email }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ barber.phone }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('app_barber_index') }}">back to list</a>

    <a href="{{ path('app_barber_edit', {'id': barber.id}) }}">edit</a>

    {{ include('barber/_delete_form.html.twig') }}
{% endblock %}
